import { Typography } from "@material-ui/core";
import React from "react";

const Header = () => {
  return (
    <header>
      <Typography gutterBottom variant="h4">
        VUTTR
      </Typography>
      <Typography variant="h5">Very useful Tools to Remember</Typography>
    </header>
  );
};

export default Header;
